name: tool-grants

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check-tool-grants:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/check_tool_grants.py
      - run: python3 -m unittest discover -s scripts
